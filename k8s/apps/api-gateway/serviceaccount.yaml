apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-gateway
  namespace: pposiraegi
  annotations:
    # IRSA: 이 Pod에만 적용될 IAM 역할 (terraform apply 후 ARN 채울 것)
    eks.amazonaws.com/role-arn: arn:aws:iam::779846782353:role/pposiraegi-api-gateway-irsa
