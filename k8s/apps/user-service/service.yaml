apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: pposiraegi
spec:
  selector:
    app: user-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 9090
      targetPort: 9090
  type: ClusterIP
